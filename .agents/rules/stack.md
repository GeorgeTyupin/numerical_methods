---
trigger: always_on
---

# Стек проекта: Численные методы на Go (Web)

Документ описывает архитектурный выбор для реализации веб-интерфейса визуализации численных методов с минимальными зависимостями.

## 🏗 Backend (Calculations & API)

* **Язык:** `Go 1.21+`
* **HTTP Router:** `go-chi/chi`
* *Почему:* Максимально близок к стандартной библиотеке `net/http`, легкий, не навязывает сложную структуру.


* **Конфигурация:** `ilyakaznacheev/cleanenv`
* *Почему:* Читает `.env` и переменные окружения напрямую в структуры Go. Самый лаконичный способ работы с конфигами.


* **Парсинг математических выражений:** `Knetic/govaluate`
* *Почему:* Позволяет пользователю вводить формулы типа `x^2 - 4` прямо в браузере. Go парсит эту строку и вычисляет значения.


* **Математика и векторы:** `Gonum`
* *Почему:* Главная библиотека для научных расчетов в Go (матрицы, дифференцирование, поиск корней).


* **Сериализация:** `encoding/json` (Стандартная библиотека)
* *Почему:* Полностью закрывает потребности передачи данных между Go и JS.



---

## 🎨 Frontend (Visualization & UI)

* **Графический движок:** `Plotly.js` (подключение через CDN)
* *Почему:* Библиотека для научных графиков. Умеет строить функции и анимировать итерации (сближение интервалов или движение касательных).


* **UI Framework:** `Tailwind CSS` (подключение через CDN)
* *Почему:* Позволяет сделать современный интерфейс (поля ввода, кнопки, карточки) без написания внешних `.css` файлов.


* **Связь с сервером:** `Native Fetch API`
* *Почему:* Современный стандарт браузеров. Не требует установки дополнительных библиотек типа Axios.



---

## 🚀 Архитектурная схема

1. **Frontend** отправляет `POST` запрос с формулой и параметрами ().
2. **Go-сервер (Chi)** принимает запрос и передает данные в сервис вычислений.
3. **Govaluate + Gonum** проводят итерации алгоритма, сохраняя координаты каждой точки шага.
4. **Backend** возвращает JSON с массивом точек для графика функции и массивом шагов алгоритма.
5. **Plotly.js** отрисовывает функцию и запускает анимацию прохождения по шагам.

---

## 📦 Сборка и Деплой

* **Одиночный бинарник:** Использование `embed` в Go для упаковки HTML/JS прямо в исполняемый файл.
* **Docker:** Минимальный образ на базе `alpine` (размер ~15-20 МБ).

---