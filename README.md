# Численные методы на Go (Web)

Веб-приложение для интерактивной визуализации численных методов. Проект превращает сложные математические расчеты в наглядный процесс, позволяя пользователям вводить функции, настраивать параметры и наблюдать за пошаговой работой алгоритмов.

## 🎯 Цель проекта

Создать масштабируемую систему для демонстрации численных методов. Архитектура приложения спроектирована как открытая экосистема, что позволяет легко добавлять новые алгоритмы оптимизации и анализа.

На этапе MVP реализованы два фундаментальных алгоритма:
1. **Метод дихотомии** (бисекции)
2. **Метод Ньютона** (касательных)

## 🛠 Технологический стек

Проект построен с акцентом на минимальные зависимости и простоту развертывания.

**Backend:**
* Go 1.21+
* `go-chi/chi` — легковесный роутер
* `ilyakaznacheev/cleanenv` — конфигурация
* `Knetic/govaluate` — парсинг математических выражений из строк
* `Gonum` — математика и вычисления
* Стандартная библиотека `encoding/json`

**Frontend:**
* Plotly.js — графики
* Tailwind CSS — стилизация интерфейса
* Native Fetch API — взаимодействие с API

## 🚀 Архитектура и принцип работы

1. Пользователь вводит математическую функцию (например, `x^2 - 4`) и параметры через веб-интерфейс.
2. Frontend отправляет POST-запрос на Go-сервер.
3. Сервер при помощи `govaluate` и `gonum` проводит итерации выбранного алгоритма, сохраняя координаты промежуточных шагов.
4. В ответ браузер получает JSON с точками для построения функции и данными для визуализации шагов.
5. Plotly.js рисует график и анимирует процесс поиска корня.

## 📦 Сборка и запуск

Проект может быть скомпилирован в один бинарный файл (с использованием `go:embed` для фронтенда) или упакован в минималистичный Docker-образ.
